{
  "name": "dominoes-react",
  "version": "0.1.1",
  "homepage": "https://kuehnert.github.io/DominoBacktracking",
  "private": true,
  "dependencies": {
    "axios": "0.24.0",
    "dotenv": "10.0.0",
    "env-path": "^1.0.7",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "react-scripts": "4.0.3"
  },
  "devDependencies": {
    "gh-pages": "3.2.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "eject": "react-scripts eject",
    "predeploy": "env-path -p .env.production npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
